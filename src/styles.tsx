import React from 'react';

import { GlobalStyles } from '@mui/material';

export const globalStyles = (
  <GlobalStyles
    styles={{
      ':root': {
        '--ui-regular-font': 'Ubuntu Sans Variable',
      },
    }}
  />
);
